name: Render Keep Alive C

on:
  schedule:
    - cron: "*/1 10-23 * * *"
    - cron: "*/1 0-1 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          echo "Pinging Render (B)..."
          curl -I --max-time 15 https://silographql.onrender.com/health || true
